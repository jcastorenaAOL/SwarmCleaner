<Window x:Class="SwarmCleaner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SwarmCleaner"
        mc:Ignorable="d"
        WindowStyle="None"
        Topmost="True"
        Background="Transparent"
        AllowsTransparency="True"
        Title="MainWindow" Height="200" Width="300"
        MouseDown="Window_MouseDown"
        ResizeMode="NoResize"
        >
    <Border Background="White" CornerRadius="10" 
            BorderBrush="Gray" BorderThickness="1"
            SnapsToDevicePixels="True">
        <Grid>
            <Button Width="200" Height="100" Background="#FFFBE3FF" Click="RefreshSwarm">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                    CornerRadius="10"
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <StackPanel Orientation="Horizontal">
                    <Image Source="Resources/logo.webp" Width="64" Height="64" />
                    <TextBlock Text="!SwarmII" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <Button.Triggers>
                    <EventTrigger RoutedEvent="Button.Click">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                      To="0.9" Duration="0:0:0.1" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                      To="0.9" Duration="0:0:0.1" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Button.Triggers>
                <Button.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Button.RenderTransform>
            </Button>
            
            <Button Content="X" HorizontalAlignment="Left" Margin="271,10,0,0" VerticalAlignment="Top" Height="20" Width="17" Click="Button_Click"/>
           
        </Grid>
    </Border>
</Window>
